# Launch TODO

- [ ] Protect Railway `chatkit` endpoint with server-side auth (for example `X-API-Key` validated in `railway-chatkit`) before public launch to prevent unauthenticated abuse/cost exposure.
- [ ] Finish native `agent-mac` parity for all required flows, then delete legacy Node agent code under `/agent` (including PM2/process-manager paths and legacy IMAP backfill scripts).
