{
  "name": "sky-ai",
  "version": "0.2.0",
  "private": true,
  "description": "Cloudflare-native Sky AI backend",
  "scripts": {
    "cf:login": "wrangler login",
    "cf:dev": "wrangler dev",
    "cf:dev:api": "wrangler dev --config wrangler.api.toml",
    "cf:dev:jobs": "wrangler dev --config wrangler.jobs.toml",
    "cf:deploy": "wrangler deploy",
    "cf:deploy:api": "wrangler deploy --config wrangler.api.toml",
    "cf:deploy:jobs": "wrangler deploy --config wrangler.jobs.toml",
    "cf:deploy:prod": "node -e \"console.error('Blocked: prod deploy disabled in dev-only mode'); process.exit(1)\"",
    "cf:deploy:api:prod": "node -e \"console.error('Blocked: prod deploy disabled in dev-only mode'); process.exit(1)\"",
    "cf:deploy:jobs:prod": "node -e \"console.error('Blocked: prod deploy disabled in dev-only mode'); process.exit(1)\"",
    "cf:d1:migrate:dev": "wrangler d1 migrations apply sky-ai-dev",
    "cf:d1:migrate:dev:api": "wrangler d1 migrations apply sky-ai-dev --config wrangler.api.toml",
    "cf:d1:migrate:dev:jobs": "wrangler d1 migrations apply sky-ai-dev --config wrangler.jobs.toml",
    "cf:d1:migrate:prod": "node -e \"console.error('Blocked: prod migrations disabled in dev-only mode'); process.exit(1)\"",
    "cf:d1:migrate:prod:api": "node -e \"console.error('Blocked: prod migrations disabled in dev-only mode'); process.exit(1)\"",
    "cf:d1:migrate:prod:jobs": "node -e \"console.error('Blocked: prod migrations disabled in dev-only mode'); process.exit(1)\"",
    "config:generate": "node scripts/generate-configs.js",
    "config:validate": "./scripts/validate-configs.sh",
    "test:mcp": "node scripts/test-mcp-oauth.mjs"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "js-yaml": "^4.1.0",
    "toml": "^3.0.0",
    "typescript": "^5.8.2",
    "wrangler": "^4.6.0"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@better-auth/core": "^1.6.20",
    "@better-auth/oauth-provider": "^1.6.20",
    "agents": "^0.7.5",
    "better-auth": "^1.6.20",
    "drizzle-orm": "^0.45.2",
    "jose": "^6.2.3"
  }
}
